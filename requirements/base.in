aiohttp<3.10
aiohttp-json-rpc
apsw
babel
click
discord.py
markdown
packaging
platformdirs
psutil
python-dateutil
python-discovery
PyYAML
rapidfuzz
Red-Commons
redbot-update
rich
schema
textual
typing_extensions
yarl
distro; sys_platform == "linux"
# https://github.com/MagicStack/uvloop/issues/702
uvloop; sys_platform != "win32" and platform_python_implementation == "CPython"

# Used by discord.py[speedup]. See Pull request #6587 for more info.
Brotli
orjson
zstandard

# --- bundled cog dependencies (fork) ---
# PyLav music stack (audio, pl*) - replaces Red-Lavalink
Py-Lav[all] @ git+https://github.com/1Example/PyLav@11dfe92734cb869eb63782065cc9070167f741fc
# dashboard, embedutils, splitorstealgame, tickets
AAA3A-utils @ git+https://github.com/AAA3A-AAA3A/AAA3A_utils.git@a8225de11678512dffba6ed65448d8fda0560c2a
fernet
markdown2
wtforms
werkzeug
markupsafe
itsdangerous
# levelup
aiocache
colorgram.py
emoji
fastapi
msgpack
plotly
pydantic>=2.11,<2.12
python-multipart
python-decouple
python-dotenv
requests
tenacity
ujson
uvicorn
jinja2
# rss
bs4
feedparser>=6.0.0
webcolors==1.3  # pinned: css3_hex_to_names removed upstream, rss/color.py needs it
filetype
# simplecasino
aiofiles
dataclasses-json
# tickets, vrtutils
chat-exporter
py-cpuinfo
speedtest-cli>=2.1.3
# aiuser
# `pydantic`, `emoji`, `tenacity`, `requests` and `aiohttp` are already pulled
# in above and cover this cog too; aiuser's own pin for pydantic is the same
# range levelup asks for, so there is nothing to reconcile.
openai>=2,<2.29
authlib>=1.6,<2
tiktoken>=0.9.0
trafilatura==2.0.0
fastembed
aiosqlite
Pillow
# Used by the cog's image generation path only, but it is imported at module
# import time, so the cog will not load without it.
modal~=1.2.5
